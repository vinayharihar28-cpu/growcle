import { MemberReportsView } from "@/features/member/components/member-reports-view";

export const metadata = {
  title: "My Reports & Scorecard | Member Workspace | Growcle",
  description: "Personal networking scorecard and participation analytics.",
};

export default function MemberReportsPage() {
  return <MemberReportsView />;
}
